<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wk.unichat">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- TODO: Intro -> Login -> Register -> Main -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".Ctrl.App">
        <activity
            android:name=".Ctrl.IntroActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Ctrl.MainActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateAlwaysHidden"/>
        <activity
            android:name=".Ctrl.LoginActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Ctrl.CreateUserActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>